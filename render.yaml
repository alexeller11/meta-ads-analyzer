services:
  - type: web
    name: meta-ads-analyzer-nvidia
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
